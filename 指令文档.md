# EPaper Keychain 指令文档

本文档基于当前代码实现整理（`src/cmd/cmd.cpp`、`include/cmd_ids.hpp`、`lib/codec/codec.cpp`）。

## 1. 协议概览

- 通信模型：**请求/响应 + 事件**。
- 传输层：`Transport`（串口、BLE、TCP 等由具体实现决定）。
- 编解码：`Codec`，负责字节流与 `Protocol::Packet` 的转换。

`Protocol::Packet` 结构：

- `type`：包类型
  - `1` = Request
  - `2` = Response
  - `3` = Event
- `session`：会话 ID（请求与响应关联）
- `cmdId`：命令 ID
- `code`：响应码（通常 `0` 成功，非 `0` 失败）
- `payload`：负载字节数组

---

## 2. 二进制帧格式

由 `Codec::encode()` / `Codec::decodeOne_()` 实现，帧格式如下：

```
+-------------------+----------------+----------------+
| FrameHeader (13B) | Payload (N B)  | CRC32 (4B)     |
+-------------------+----------------+----------------+
```

### 2.1 FrameHeader 字段（共 13 字节）

按结构体顺序直接内存拷贝，字段如下：

1. `magic` (uint32) 固定值：`0x31434443`
2. `type` (uint8)
3. `session` (uint16)
4. `cmdId` (uint16)
5. `code` (uint16)
6. `len` (uint16) 负载长度

> 当前实现运行于 ESP 平台，按小端序处理（主机字节序）。

### 2.2 校验

- CRC 范围：`FrameHeader + Payload`
- CRC 算法：CRC-32（多项式 `0xEDB88320`）
- CRC 字段位置：帧尾 4 字节

### 2.3 默认限制

- `maxPayloadBytes = 4096`
- `maxBufferBytes = 8192`

---

## 3. 命令定义（cmdId）

定义位置：`include/cmd_ids.hpp`

| 命令名 | cmdId (hex) | 说明 |
|---|---:|---|
| Info | `0x0001` | 健康检查 |
| LogHistory | `0x0101` | 获取日志历史 |
| LogLevelSet | `0x0102` | 设置日志级别 |
| LogClear | `0x0103` | 清空日志历史 |

---

## 4. 指令详解

## 4.1 Info (`0x0001`)

- 请求：
  - `type = Request`
  - `payload = 空`
- 响应：
  - `type = Response`
  - `code = 0`
  - `payload = "ok"`（2 字节）

## 4.2 LogHistory (`0x0101`)

- 请求：
  - `type = Request`
  - `payload = 空`
- 响应：
  - `type = Response`
  - `code = 0`
  - `payload = 历史日志原始字节`
- 备注：
  - 返回长度上限为 `min(Logger::history_size(), codec.maxPayloadBytes)`
  - 若无日志，返回空 payload

## 4.3 LogLevelSet (`0x0102`)

- 请求：
  - `type = Request`
  - `payload[0] = level`
- 支持的 `level`：
  - `0` Error
  - `1` Warn
  - `2` Info
  - `3` Debug
  - `4` Verbose
  - `255` Off
- 响应：
  - 成功：`code = 0`，`payload = [level]`
  - 失败：`code = 1`，`payload = 空`
- 失败条件：
  - `payload` 长度 < 1
  - level 不在上述允许值内

## 4.4 LogClear (`0x0103`)

- 请求：
  - `type = Request`
  - `payload = 空`
- 响应：
  - `type = Response`
  - `code = 0`
  - `payload = 空`

---

## 5. 交互约定

- 仅当收到 `Request` 类型时，设备侧命令处理器才会执行。
- 建议主机侧：
  - 每次请求使用递增 `session`；
  - 通过 `session + cmdId` 关联响应；
  - 对 `code != 0` 统一走错误处理。
- 未注册 `cmdId`：设备不返回业务响应（内部仅记录 debug 日志）。

---

## 6. 示例流程

### 6.1 心跳检查

1. 主机发送 `Request(cmdId=0x0001, payload=空)`
2. 设备返回 `Response(cmdId=0x0001, code=0, payload="ok")`

### 6.2 设置日志级别为 Debug

1. 主机发送 `Request(cmdId=0x0102, payload=[0x03])`
2. 设备返回 `Response(cmdId=0x0102, code=0, payload=[0x03])`

### 6.3 拉取并清空日志

1. 主机发送 `Request(cmdId=0x0101)` 获取日志
2. 设备返回日志字节流
3. 主机发送 `Request(cmdId=0x0103)` 清空日志

---

## 7. 异常与排查

编解码层常见错误（`Codec::Error`）：

- `BadMagic`：帧头魔数错误
- `BadLength`：声明长度超限
- `BadChecksum`：CRC 不一致
- `BufferOverflow`：输入超出缓冲限制
- `NeedMore`：数据未收齐，继续收包即可

建议：

- 主机端先做帧级校验（magic/len/crc）；
- 出现连续 `BadMagic` 时，尝试按字节滑动重同步；
- 长包场景确保不超过 `maxPayloadBytes`。
